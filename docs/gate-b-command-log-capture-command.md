# Gate B Command Log Capture Command

Run:

```bash
npm run review:gate-b:logs
```

Purpose:

- capture current `status:gate-b` output;
- capture current `guard:sr07` output;
- write both into `review/spatial-lab/command-logs/`;
- keep Gate B pending until human recordings and a human decision exist.

